# map-compiler (skeleton)

CLI skeleton for the map compiler based on `spec/09_map-compiler.md`.

## Run

```bash
node apps/map-compiler/cli/cli.js compile --smap ./maps/example.smap --out-dir ./compiled
```
